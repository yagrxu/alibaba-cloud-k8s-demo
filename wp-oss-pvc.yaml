apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pgp-container
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2Gi
  selector:
    matchLabels:
      alicloud-pvname: pgp-container
  storageClassName: oss
  volumeName: pgp-container
